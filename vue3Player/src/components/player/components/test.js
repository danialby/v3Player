const historyTouchY = ref([]);
const playingObject = ref({});
const sliderCustomize = ref({
    "processStyle": {"backgroundColor": "var(--Accent)"},
    "bgStyle": {"backgroundColor": "rgba(0,0,0,0.2)"},
    "dotStyle": {"backgroundColor": "var(--Accent)", "zIndex": 10000},
    "disabledStyle": {"padding": 0, "opacity": 1, "backgroundColor": "rgba(0,0,0,0.2)"}
});